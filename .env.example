# Copie este arquivo para ".env" e preencha com os valores reais.
# NUNCA envie o arquivo ".env" para o Git (ele já está no .gitignore).

DB_HOST=localhost
DB_NAME=tcc
DB_USER=root
DB_PASS=
APP_ENV=development
APP_URL=http://localhost/mon_agua
